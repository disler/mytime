/**
 * Purpose: Handles saving and loading of user state from their computer
 */

import { UserConfig } from "../types";

export function loadConfig(): UserConfig {
    return {}
}

export function saveConfig(config: UserConfig) {
    
}